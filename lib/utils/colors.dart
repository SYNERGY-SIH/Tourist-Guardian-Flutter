import 'package:flutter/material.dart';

class AppColors {
  // --- NEW LIGHT THEME PALETTE ---

  // Background colors
  static const Color background = Colors.white;
  static const Color surface = Color(0xFFF5F5F7); // For cards, text fields

  // Primary action colors
  static const Color primary = Color(0xFF007AFF); // A vibrant, clean blue
  static const Color secondary = Color(0xFF5856D6); // A complementary purple

  // Text colors
  static const Color text = Color(0xFF1D1D1F); // A strong, near-black for text
  static const Color textSecondary = Colors.grey; // For subtitles or hints

  // State colors
  static const Color success = Color(0xFF34C759);
  static const Color error = Color(0xFFFF3B30);
}